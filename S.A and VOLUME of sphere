#include <stdio.h>
main()
{
    const float pi=3.14;
    float r,area,volume;
    printf("enter radius=");
    scanf("%f",&r);
    area=4*pi*r*r;
    volume=(4/3*pi*r*r*r);
    printf("area of circle is %f and volume of circle is %f",area,volume);
    

}
